# Public site URL — used for canonical tags, Open Graph, sitemap and robots.
NEXT_PUBLIC_SITE_URL=https://gnanachandra.com

# Optional. Any endpoint that accepts a JSON POST (email service, automation
# platform, CRM). Contact submissions are forwarded here. If unset, the form
# still works and submissions are logged server-side.
CONTACT_WEBHOOK_URL=
